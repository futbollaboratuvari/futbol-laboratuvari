name: Card Flow

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "data/live-matches.json"
      - "scripts/card-flow-lite.js"
      - "ops/main-run.txt"

permissions:
  contents: write

concurrency:
  group: futbol-main-data-writer
  cancel-in-progress: false

jobs:
  card-flow:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Repair input conflicts
        run: node scripts/repair-conflict-markers.js

      - name: Fill card data
        run: node scripts/card-flow-lite.js

      - name: Commit card data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/daily-coupons.json || true
          if git diff --cached --quiet; then
            echo "No card changes"
            exit 0
          fi
          git commit -m "Update card flow data"
          for attempt in 1 2 3; do
            if git pull --rebase --autostash origin main && node scripts/validate-json-no-conflicts.js && git push origin HEAD:main; then
              exit 0
            fi
            git rebase --abort || true
            sleep $((attempt * 2))
          done
          echo "Card Flow push failed after retries"
          exit 1
