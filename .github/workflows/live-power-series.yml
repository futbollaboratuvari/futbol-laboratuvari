name: Live Power Series

on:
  workflow_dispatch:
  schedule:
    - cron: "12,42 * * * *"
  push:
    branches:
      - main
    paths:
      - "ops/live-power-trigger.txt"
      - ".github/workflows/live-power-series.yml"
      - "scripts/update-live-power-series-v2.js"
      - "scripts/live-match-analysis-robot.js"
      - "scripts/validate-live-match-analysis.js"
      - "tests/live-match-analysis-robot.test.js"
      - "live-power-center-v1.js"

permissions:
  contents: write

concurrency:
  group: futbol-live-power-writer
  cancel-in-progress: false

jobs:
  live-power:
    runs-on: ubuntu-latest
    env:
      LIVE_POWER_MAX_MATCHES: "8"

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Test live power engine
        run: |
          node tests/live-power-series.test.js
          node tests/live-power-core.test.js
          node tests/live-match-analysis-robot.test.js

      - name: Collect verified free-source live power snapshots
        run: node scripts/update-live-power-series-v2.js

      - name: Build live match analysis robot output
        run: node scripts/live-match-analysis-robot.js

      - name: Validate output JSON
        run: |
          node scripts/validate-live-power-series.js
          node scripts/validate-live-match-analysis.js

      - name: Commit live power output
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git pull --rebase --autostash origin main
          node tests/live-power-series.test.js
          node tests/live-power-core.test.js
          node tests/live-match-analysis-robot.test.js
          node scripts/update-live-power-series-v2.js
          node scripts/live-match-analysis-robot.js
          node scripts/validate-live-power-series.js
          node scripts/validate-live-match-analysis.js
          git add data/live-power-series.json data/live-match-analysis.json
          if git diff --cached --quiet; then
            echo "No live power changes"
            exit 0
          fi
          git commit -m "Update verified free-source live power series"
          for attempt in 1 2 3 4 5; do
            if git pull --rebase --autostash origin main && node tests/live-power-series.test.js && node tests/live-power-core.test.js && node tests/live-match-analysis-robot.test.js && node scripts/validate-live-power-series.js && node scripts/validate-live-match-analysis.js && git push origin HEAD:main; then
              exit 0
            fi
            git rebase --abort || true
            sleep $((attempt * 3))
          done
          echo "Live Power push failed after retries"
          exit 1

